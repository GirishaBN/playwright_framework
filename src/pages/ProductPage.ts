import { Locator, Page } from "@playwright/test";

export class ProductPage{
/* private readonly prodcutName:Locator;
constructor(page:Page)
{
    this.prodcutName=page.getByRole('heading', { name: `${prodcutName}`, level: 1 })

} */
}